name: Mobile CI

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read

jobs:
  startup:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Diagnostic
        run: echo "VJR 221 Mobile CI startup OK"
