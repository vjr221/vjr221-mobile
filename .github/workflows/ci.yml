name: Mobile CI (deprecated, disabled)

# DEPRECATED: superseded by .github/workflows/mobile-ci.yml
# This workflow used to run on every push/PR and duplicated mobile-ci.yml's
# job, but relied on `npm ci` against a package-lock.json that is no longer
# kept in sync with package.json (mobile-ci.yml uses `npm install` instead).
# It has been disabled here (no automatic triggers) rather than deleted,
# since this API cannot delete files. Safe to delete this file manually.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated and disabled. See mobile-ci.yml."
